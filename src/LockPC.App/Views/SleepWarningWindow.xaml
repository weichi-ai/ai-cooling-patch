<Window x:Class="LockPC.App.Views.SleepWarningWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="今日 AI 亲密额度即将用完"
        Width="560" Height="410"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="True"
        Background="#F5F7FA">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Width="48" Height="48" CornerRadius="14" Background="#E3FAF6" HorizontalAlignment="Left">
            <TextBlock Text="Ⅱ" FontSize="22" FontWeight="Bold" Foreground="#22BFA9" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>
        <StackPanel Grid.Row="1" Margin="0,18,0,0">
            <TextBlock Text="今日 AI 亲密额度即将用完。" FontSize="23" FontWeight="SemiBold" Foreground="#101525" />
            <TextBlock x:Name="MessageText" Margin="0,8,0,0" TextWrapping="Wrap" Foreground="#667085" FontSize="14" LineHeight="22" />
        </StackPanel>
        <TextBlock Grid.Row="2" x:Name="CountdownText" Text="00:30" FontSize="38" FontWeight="SemiBold" FontFamily="Consolas" Foreground="#101525" Margin="0,20,0,12" />
        <StackPanel Grid.Row="3" x:Name="DelayPanel">
            <TextBlock x:Name="DelayHintText" Text="今晚还可以申请最后一次延迟，最长 30 分钟。" Foreground="#667085" FontSize="12" Margin="0,0,0,8" />
            <WrapPanel x:Name="DelayButtonsPanel">
                <Button Content="再聊 5 分钟" Tag="5" Click="Delay_Click" Margin="0,0,8,0" />
                <Button Content="再聊 10 分钟" Tag="10" Click="Delay_Click" Margin="0,0,8,0" />
                <Button Content="再聊 15 分钟" Tag="15" Click="Delay_Click" Margin="0,0,8,0" />
                <Button Content="再聊 30 分钟" Tag="30" Click="Delay_Click" />
            </WrapPanel>
        </StackPanel>
        <Button Grid.Row="4" Content="按时休息" HorizontalAlignment="Right" Click="Acknowledge_Click" />
    </Grid>
</Window>
