# AI退烧贴产品需求规格（v0.6）

> 状态：第一轮需求已确认，剩余边界待确认  
> 目标平台：Windows 10/11 桌面端  
> 产品定位：面向 AI 与 PC 重度使用者，通过定时专注、强制离屏休息和睡眠保护，帮助用户避免持续上头并恢复正常节奏。

## 1. Problem Statement（问题陈述）

长时间使用 PC 时，用户容易因为即时反馈和持续刺激而推迟离开电脑，导致计划中的专注活动无法开始，或在深夜继续使用电脑而压缩睡眠时间。普通提醒和可随手关闭的番茄钟约束力不足，用户需要一种在预定时间内主动让出电脑控制权、到期自动恢复的工具。

产品需要解决两个不同强度的问题：

1. **番茄专注与强制离屏休息**：用户在 M 分钟专注期内正常使用电脑；专注结束后，电脑被锁定 N 分钟，引导用户离开屏幕休息；休息结束后自动进入下一轮专注。
2. **睡眠保护**：每天到达指定时间后强制停止使用电脑，直到预定结束时间；只允许开始前延迟一次，不允许生效后提前解锁。

产品成败不只取决于倒计时界面，还取决于多显示器覆盖、输入阻断、重启恢复、跨零点判断以及发生故障时不会形成永久锁定。

## 2. Proposed Solution（方案描述）

AI退烧贴（内部项目代号 LockPC）使用覆盖所有显示器的应用级全屏阻断层完成定时锁定，并由 Windows 后台服务保存权威计划、驱动状态切换和恢复异常退出的覆盖层。番茄钟的专注期允许用户正常操作电脑，休息期显示半透明锁定界面；睡眠保护期显示不透明锁定界面。两种锁定界面均显示剩余时间和治愈系背景。

这里的“锁屏”不是 Windows 登录锁屏。Windows 登录锁屏无法由应用在指定时刻安全地替用户自动解锁，因此本产品采用可自动解除的应用级锁定方案。

### 2.1 产品目标

1. 用户能在专注期正常使用电脑，并在每个专注期结束后被强制引导离开屏幕休息。
2. 用户能设定每日或指定星期的睡眠保护时间段。
3. 锁定期间界面平静、信息明确，并准确显示剩余时间。
4. 程序重启、电脑睡眠唤醒或跨越零点后仍能恢复正确状态。
5. 专注模式不防管理员主动绕过；睡眠保护不提供产品内提前解锁入口，并在重启后继续执行。

## 3. Non-goals（明确不做的事）

- 不替代 Windows 账户密码、BitLocker 或企业设备管理。
- 不承诺防御管理员、安全模式、重装系统等主动绕过。
- 不做网站/应用黑名单、家长远程控制、跨设备同步。
- 不收集键盘内容、屏幕内容或用户隐私数据。

## 4. 核心概念

- **应用级锁定**：置顶、无边框、覆盖所有显示器的窗口；拦截普通鼠标键盘交互，不允许从界面关闭。
- **专注期**：用户正常使用电脑的 M 分钟；番茄钟在后台或小型计时界面持续运行，不阻断输入。
- **休息期**：专注期结束后的 N 分钟；所有显示器被半透明覆盖，电脑不可操作，引导用户离开屏幕，结束后自动进入下一专注期。
- **睡眠保护期**：从每日开始时间到结束时间的不透明、高强度阻断时段，不提供提前解锁入口。
- **自动安全兜底**：仅用于处理状态损坏或程序故障，由程序根据受保护的有效结束时间自动恢复；不是用户可调用的提前解锁功能。

## 5. 功能需求与用户流程

### 5.1 首次启动

1. 展示产品能力边界与风险说明。
2. 用户选择是否开机自启。
3. 用户可选择内置治愈系图片或纯色/渐变背景。
4. 进入主界面。

### 5.2 专注模式

#### 配置

- 专注时长 M：快捷选项 15、25、30、45、50、60 分钟；自定义范围 15–90 分钟。
- 休息时长 N：快捷选项 5、10、15 分钟；自定义范围建议 1–30 分钟，默认 10 分钟。
- 轮数：支持 1–12 轮或“持续循环”，默认 4 轮。
- 开始前确认页显示完整计划和预计结束时间。

#### 运行

1. 用户点击“开始专注”。
2. 建议提供 5 秒最终倒计时，期间可直接取消；计划开始后可填写不少于 5 个字符的理由取消。
3. 进入专注期：桌面和应用可正常操作；托盘或小型置顶计时器显示当前轮次、专注剩余时间和下次休息开始时间。
4. 专注期结束后立即进入休息期：所有显示器出现半透明覆盖层，鼠标和普通键盘操作被阻断。
5. 休息锁定界面显示治愈系背景、当前轮次、`距离下轮专注还有 MM:SS` 和下一轮开始时间。
6. 休息倒计时结束后自动解除覆盖层并开始下一轮专注。
7. 最后一轮专注结束后仍执行一次完整休息；最后一次休息结束后显示完成页和本次统计，不再开始下一轮。

#### 暂停与退出

- 一轮计划开始后不提供暂停或无理由跳过。
- 专注期电脑可正常使用；用户可手动取消整个计划，但必须填写不少于 5 个字符的理由。
- 休息锁定期可选择提前结束，但必须填写不少于 5 个字符的理由；理由输入完成前仍保持锁定。
- 每次计划取消和休息提前结束均记录事件类型、时间、理由、当前轮次、计划参数和原定剩余时间。

### 5.3 睡眠保护模式

#### 配置

- 启用开关。
- 开始时间与结束时间，精确到分钟，支持跨零点，例如 23:30–06:00。
- 生效星期：周一至周日多选，默认每天。
- 提前提醒：默认 30 秒；建议可选 30 秒、1 分钟、5 分钟。
- 每个计划窗口仅允许延迟一次；提供 5、10、15、30 分钟选项，最大不得超过 30 分钟。
- 时区：默认跟随系统时区。

#### 运行

1. 到开始时间前弹出置顶提醒：“睡眠保护将在 00:30 后开始，请保存重要文件。”
2. 提醒倒计时内允许点击“我知道了”或选择一次延迟；延迟最多 30 分钟，不能取消当次计划，也不能二次延迟。默认只推迟开始时间，原定结束时间不变。
3. 到点后进入不透明全屏覆盖层，覆盖所有显示器。
4. 显示治愈系背景、当前时间、`距离解除还有 HH:MM:SS` 和预计解除时间。
5. 到结束时间自动解除覆盖层，恢复正常桌面操作。
6. 若电脑在保护期内启动、登录或从睡眠中唤醒，应在后台服务启动后立即恢复保护。
7. 睡眠保护期间不提供暂停、退出、恢复码或确认短语解锁；计划设置也不能在生效期间修改。
8. 允许 Windows 按当前电源设置关闭显示器；键鼠唤醒后保护层仍然生效。

## 6. 界面与视觉要求

### 6.1 主界面

- 首页提供“专注模式”“睡眠保护”两个主要入口。
- 清晰展示下一次睡眠保护开始时间和当前专注计划。
- 设置页包含启动项、背景图、声音、数据与隐私。

### 6.2 休息锁定覆盖层

- 覆盖层透明度建议默认 55%，可配置范围建议 30%–80%。
- 桌面可见但不可点击；覆盖层本身不显示可退出控件。
- 主屏居中显示“请离开屏幕休息”、休息倒计时及下一轮开始时间；副屏显示简化倒计时，避免不同屏幕出现可操作空隙。
- 主屏提供低强调度的“提前结束休息”入口；确认前必须填写不少于 5 个字符的理由。
- 支持分辨率、缩放比例、横竖屏及显示器热插拔。

### 6.3 睡眠覆盖层

- 完全不透明，不泄露桌面内容。
- 背景图上增加暗色蒙层，保证文字对比度。
- 图片默认使用本地内置资源；用户自定义图片只保存在本机。
- 可提供“降低亮度观感”的深色主题，但不直接修改显示器硬件亮度。

### 6.4 无障碍与防烧屏

- 倒计时至少满足基本对比度和字号要求。
- 支持关闭动画、关闭提示音。
- 长时睡眠保护时，文字位置可轻微缓慢移动；也可允许系统正常息屏，唤醒后立即重新显示保护层。

## 7. Technical Constraints（技术约束）

### 7.1 两级锁定强度

- 全屏置顶覆盖所有显示器。
- 隐藏任务栏和系统托盘区域。
- 屏蔽覆盖层内常见操作，如 `Alt+F4`、`Alt+Tab`、Windows 键和鼠标点击。
- 后台守护服务检测覆盖进程；若异常关闭，在保护仍有效时自动重启。
- 应用重新启动时依据持久化状态恢复剩余时间，不重置计时。
- **番茄钟休息锁定**：不提供普通关闭或无理由跳过入口；允许通过受控理由流程提前结束，且接受用户通过管理员手段主动绕过。
- **睡眠保护**：不提供暂停、退出、恢复码或确认短语；计划生效期间不能修改或禁用。后台服务、状态文件和进程间通信需使用系统权限及访问控制降低普通绕过风险。

### 7.2 Windows 平台边界

- `Ctrl+Alt+Delete` 安全桌面。
- 管理员结束服务、卸载软件或修改本机数据。
- 重启进入安全模式、修改系统时间或关闭电源。
- 远程桌面、虚拟桌面和部分系统级辅助工具可能产生额外边界情况。

睡眠保护中的“不允许破解”定义为：产品不提供任何提前解除路径，并抵抗当前用户权限下的普通关闭、快捷键、进程崩溃和重启绕过。拥有本机管理员权限或物理控制权的用户始终可以通过系统级手段绕过，因此产品文案不得宣称“绝对无法破解”。如需对管理员也实施组织级限制，必须另行采用 Windows kiosk、组策略或设备管理方案。

### 7.3 无手动解锁下的故障安全

- 每次睡眠保护必须在启动前写入明确、可校验且不可通过普通 UI 修改的结束时间。
- 单次睡眠保护时长必须小于 24 小时，开始时间不能等于结束时间。
- 配置或运行状态写入必须采用原子更新，并保留最后一个可验证版本。
- 状态损坏时不得创建没有结束时间的锁定；服务应回退到最后一个可验证的计划结束时间。
- 自动故障兜底只负责在可验证结束时间到达后解除，不得在有效保护期内被用户主动触发。
- 安装、升级和卸载不得在睡眠保护生效期间执行。

## 8. 计时与状态规则

### 8.1 计时原则

- 持久化保存阶段的绝对开始/结束时间以及基于单调时钟的运行参考，避免只依赖界面倒计时。
- UI 每秒刷新；阶段切换误差目标不超过 1 秒。
- 系统时间向前/向后调整时，睡眠计划按当前本地日历时间重新计算；正在运行的专注倒计时不得因修改系统时间而缩短。

### 8.2 重启、睡眠和崩溃

- 专注期间睡眠：默认计入专注时长；唤醒后按原结束时刻继续或结束。
- 睡眠保护期间关机/重启：重新登录后若仍在保护时间窗内，立即恢复保护。
- 错过睡眠开始事件：启动时判断当前是否位于保护窗口，而不是只依赖定时事件。
- 错过结束事件：恢复运行后若已过结束时间，不再显示保护层。
- 覆盖进程崩溃：守护进程应在目标 2 秒内重新拉起。

### 8.3 跨零点与星期定义

对于 23:30–06:00 这类计划，星期归属按“开始日”计算。例如勾选周五，表示周五 23:30 至周六 06:00 生效。

## 9. 通知与声音

- 专注开始、阶段切换和完成可播放柔和提示音，默认开启，可关闭。
- 睡眠保护开始前使用置顶应用内通知，不能只依赖 Windows 普通通知。
- 睡眠保护解除默认静默，避免 06:00 主动吵醒用户；可选提示音。
- 覆盖层不播放持续背景音乐，除非后续明确加入。

## 10. 数据与统计

首版建议本地记录：

- 专注计划开始/结束时间、设定时长、完成轮数。
- 正常完成、异常退出尝试和守护恢复事件。
- 计划取消与休息提前结束的事件类型、发生时间、理由、当前轮次、计划参数和原定剩余时间。
- 睡眠保护实际开始/解除时间和异常恢复事件。

统计页可显示今日/本周完成时长、完成率和锁定恢复次数。所有数据默认只存本机，并提供清空记录功能。

## 11. 技术实现建议

### 11.1 进程结构

1. **桌面 UI 进程**：配置、统计、托盘和覆盖层。
2. **Windows 后台服务**：保存权威状态、执行计划、监测并恢复覆盖层。
3. **本地存储**：保存设置、计划、当前状态和审计记录。
4. **进程间通信**：仅允许本机、带身份校验；UI 不能通过普通关闭命令终止有效保护。

### 11.2 推荐实现方向

- 当前桌面版本采用 C# + .NET 8 + WPF，适合 Windows 多屏、托盘、服务和系统 API 集成。
- 若更重视现代 UI：C# + .NET 8 + WinUI 3，但服务与窗口生命周期集成复杂度略高。
- 不建议首版用纯 Electron：安装体积、常驻内存以及系统级输入/服务集成成本更高。
- 安装器需支持开机启动、服务安装/升级和完整卸载。安装后台服务需要管理员权限；睡眠保护生效期间拒绝卸载。

## 12. 非功能要求

- 支持 Windows 10 22H2、Windows 11 当前受支持版本。
- 空闲时 CPU 平均占用目标低于 1%，内存目标低于 150 MB。
- 不联网也能完成全部核心功能。
- 本地配置写入应原子化，断电后不得出现无结束时间的永久锁定状态。
- 每个锁定状态都必须包含可校验的结束时间和故障兜底上限。
- 程序日志不得记录键盘输入、桌面截图或用户文件内容。

## 13. Success Criteria（成功标准）

当前版本达到以下全部可验证条件时视为成功。

### 13.1 番茄专注模式

- 可选择 15、25、30、45、50、60 分钟预设时长和 15–90 分钟自定义时长。
- 专注期内桌面及其他应用可正常操作，计时器持续显示或可从托盘查看。
- 专注倒计时结束后，所有已连接显示器均被半透明覆盖，普通鼠标键盘无法操作桌面。
- 休息锁定倒计时准确；提前结束时必须填写不少于 5 个字符的理由，成功保存后才解除并开始下一轮专注。
- 专注期取消整个计划时必须填写不少于 5 个字符的理由，成功保存后才终止计划。
- 计划取消和休息提前结束记录可在本机记录页查看，并包含时间、理由、轮次及剩余时间。
- 休息期关闭覆盖进程或重启应用后，在有效期内能够恢复锁定和剩余时间。
- 最后一轮专注后仍完成一次休息，之后结束计划，不再自动开始下一轮。

### 13.2 睡眠保护

- 支持同日和跨零点时间段、星期选择。
- 开始前按配置显示保存文件提醒，并且每个计划窗口最多延迟一次、最长 30 分钟。
- 到点后所有显示器均被不透明覆盖，并显示准确的解除倒计时。
- 在保护窗口内重启或唤醒后自动恢复；到结束时间自动解除。
- 生效期间不存在产品提供的暂停、退出、恢复码或修改当前计划入口。
- 使用普通用户可执行的关闭窗口、快捷键、切换窗口和结束 UI 进程测试，均不能解除保护；UI 进程退出后目标 2 秒内恢复。
- 跨越夏令时、手动改时和错过触发事件时不会形成永久锁定。

### 13.3 可靠性与性能

- 配置损坏、服务异常和 UI 崩溃时存在明确兜底，不产生无限期覆盖。
- 日志能够说明一次异常锁定和解除的原因。
- 阶段切换和自动解除相对目标时间误差不超过 1 秒。
- 空闲时 CPU 平均占用低于 1%，内存占用低于 150 MB。
- 无网络连接时全部核心功能正常可用。

## 14. 建议版本范围

### v0.1

- 单机、单用户。
- 专注循环与睡眠计划。
- 多显示器覆盖层。
- 内置/自定义本地背景图。
- 后台守护、重启恢复和自动故障兜底。
- 基础本地统计。

### 后续版本

- 应用/网站黑名单或白名单。
- 长休息规则（例如每 4 轮休息 20 分钟）。
- 临时例外日期、节假日计划。
- 家庭监督、跨设备同步、云端统计。
- 更强的 kiosk/组织管理模式。

### 关于与版本更新

- “关于”页显示产品介绍、当前版本号和本地版本更新记录。
- 应用启动后自动调用 GitHub Releases 最新公开版本接口进行比较。
- 检测到新版本时在“关于”标签和页面内提示，并提供 GitHub Release 下载按钮。
- 当前版本已是最新版本时明确显示“已是最新版本”。
- 未配置仓库地址、没有公开 Release 或网络不可用时显示可恢复状态，不影响全部离线核心功能。

## 15. 待产品确认的问题

以下问题会直接影响交互和技术设计：

1. **延迟后的结束时间**：睡眠保护延迟开始后，是否保持原定结束时间不变？当前默认不顺延结束时间。
2. **循环扩展**：除默认 4 轮外，首版是否需要无限循环、每 4 轮一次长休息？
3. **最后一轮休息**：最后一轮专注结束后是否仍强制完成一次休息？当前默认需要，休息完成后结束整个计划。
4. **多用户与远程桌面**：首版是否只考虑一台电脑上的当前 Windows 用户？
5. **关闭/重启策略**：锁定期可通过 `Ctrl+Alt+Delete` 进入系统安全界面，是否只要求重启登录后继续恢复锁定？当前默认如此。
6. **图片来源**：只提供内置图片和用户本地图片，还是需要联网每日更新？当前建议首版完全离线。
7. **统计需求**：是否保留基础统计页，还是首版只完成锁定能力？

## 16. 已确认决策与默认值

- 采用 C#/.NET 8 + WPF。
- 定义为应用级强制覆盖，不宣称绝对系统锁定。
- 专注期正常使用电脑；计划可填写不少于 5 个字符的理由取消。休息期锁定电脑，但可填写不少于 5 个字符的理由提前结束；默认 4 轮、每轮休息 10 分钟。
- 睡眠保护允许显示器正常息屏。
- 睡眠保护开始前允许延迟一次，最长 30 分钟。
- 睡眠保护不提供任何产品内提前解锁功能。
- 开机自启并使用后台服务；保护窗口内启动时立即恢复。
- 全部核心功能离线运行，数据只存本机。
